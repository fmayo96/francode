[project]
name = "francode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fmayo96", email = "francomayo96@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.16.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
francode = "francode:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
